specVersion: "v1beta"
serviceId: "GeoLegendsClient"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "GeoLegendsClient-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
